export interface UserDetails {
    /** Email address of user. */
    email: string;
    /** ID of user on the remote platform. */
    id: string;
    /** Display name. */
    name: string;
    /** Profile picture URL. */
    picture: string;
}
